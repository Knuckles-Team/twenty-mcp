# Twenty MCP

Twenty CRM REST/GraphQL MCP Server for Agentic AI!

## Environment Variables
- `TWENTY_URL`: The base service URL.
- `TWENTY_TOKEN`: The API / bearer token for authentication.

## CLI Usage
```bash
python -m twenty_mcp --help
```
